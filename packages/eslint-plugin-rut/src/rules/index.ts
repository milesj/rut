import noAct from './no-act';
import noInternals from './no-internals';
import requireRenderGenerics from './require-render-generics';

export default {
  'no-act': noAct,
  'no-internals': noInternals,
  'require-render-generics': requireRenderGenerics,
};
